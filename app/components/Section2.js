import Services from "./Services";
import Channel from "./Channel";

const Section2 = () => {
  return (
    <div className="py-8" style={{ background: 'linear-gradient(to bottom, rgba(247, 108, 40, 1), rgba(204, 83, 22, 1), rgba(202, 84, 24, 1))' }}>
      <Services />
      <Channel />
    </div>
  );
};

export default Section2; 